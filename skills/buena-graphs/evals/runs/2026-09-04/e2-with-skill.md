(Report of an agent run with tools: followed step 4, installed
graph-uptime from the local registry item via `npx shadcn@latest add`
(raw GitHub URL 404s pre-launch, matching the skill's troubleshooting
row), rendered <GraphUptime client:visible> on a /status page with 42
days / 6 degraded / 2 down and percent 90, verified glyphs and sr-only
summary in built HTML. Found a real consumer edge: typed targets split
the component tree across Astro dual-renderer boundaries -- now a
troubleshooting row. NOTE: the run escaped the intended sandbox and
executed against a real repo; all side effects were reverted, the
findings kept.)
